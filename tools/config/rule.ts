export const BRANCH_REGEX =
  /^(feature|fix|refactor|hotfix|docs)\/[a-z0-9-_]+$/;

export const PR_TITLE_REGEX =
  /^\[(feature|fix|refactor|chore|hotfix|test|docs)\]\s.+$/;

export const SNAKE_CASE_REGEX =
/^[a-z0-9]+(?:_[a-z0-9]+)*$/;